Main concerns:

- Implement document state save and loading
- Write complete installing documentation (for windows)
- Add system installer

Minor fixes/improvements:

- replace malloc and free with glib's memory handling functions
- implement a workaround for when the user closes the app with a save file dialog open
